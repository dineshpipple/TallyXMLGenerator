// using System.Xml.Serialization;
// XmlSerializer serializer = new XmlSerializer(typeof(STOCKITEM));
// using (StringReader reader = new StringReader(xml))
// {
//    var test = (STOCKITEM)serializer.Deserialize(reader);
// }


